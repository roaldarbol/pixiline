# Pixi environment (rebuilt from pixi.toml / pixi.lock)
.pixi/

# Python
__pycache__/
*.py[cod]
.ruff_cache

# Tests / coverage
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# Output artefacts (the output dir is chosen by the user, but ignore ./output)
output/

# GUI session logs
logs/

# Example pipeline workspace - kept local while we experiment; not part of the
# pixiline manager repo. Remove this line to start tracking it (or move it out).
sleep-staging/

# OS cruft
Thumbs.db
.DS_Store
